  node {  
      stage('Build') { 
          checkout([$class: 'GitSCM', branches: [[name: '*/master']], 
          doGenerateSubmoduleConfigurations: false, extensions: [], submoduleCfg: [], 
          userRemoteConfigs: [[url: 'https://gitlab.direa.synology.me/cruzapim-k8s/authserver.git']]])
      }
      stage('Test') { 
          // 
      }
      stage('Deploy') { 
          // 
      }
  }
